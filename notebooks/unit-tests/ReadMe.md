# Thoughts
I am unsure if we would have layer based folders to mirror the unit tests here. it may depend on if we would just run the lot. Or if in fact we would use notebooks but would use some kind of built in test runner.
